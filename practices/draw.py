#KH drawing starter
import turtle
def draw_branch():
    if len > 5:
        for i in range(3):
            turtle.forward
            draw_branch(len/3)
            turtle.backwards
            turtle.right(60)

turtle.Turtle
turtle.speed(1)
turtle.color("lightblue")
turtle.goto(100, 50)
turtle.pendown()

def main():
    for i in range(6):
        draw_branch(100)
        turtle.right(60)
        turtle.hide

turtle.done()


